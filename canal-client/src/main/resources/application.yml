
# 数据库同步服务端口
server:
  port: 10000

# 服务名
spring:
  application:
    name: canal-client-10000
  profiles:
    active: dev



    #本地数据库连接测试，和远程服务器上协同
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/canal?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2B8
    username: root
    password: pyb